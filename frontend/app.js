const form=document.querySelector('#research-form'),question=document.querySelector('#question'),result=document.querySelector('#result');
const esc=s=>String(s).replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));
form.addEventListener('submit',async e=>{e.preventDefault();const b=form.querySelector('button');b.disabled=true;b.innerHTML='Investigating <span>···</span>';result.className='result';result.innerHTML='<div class="loading"><b>AGENT ACTIVE</b><p>Defining scope, selecting sources, and preserving uncertainty…</p></div>';try{const res=await fetch('/api/research',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({question:question.value})});if(!res.ok)throw Error('Research request failed');render(await res.json())}catch(err){result.innerHTML=`<div class="error"><b>Research paused</b><p>${esc(err.message)}. Please try again.</p></div>`}finally{b.disabled=false;b.innerHTML='Research <span>↗</span>'}});
function render(r){const ev=r.events.map(e=>`<li class="${e.status}"><time>${new Date(e.timestamp).toLocaleTimeString([],{hour:'2-digit',minute:'2-digit'})}</time><b>${esc(e.title)}</b><small>${esc(e.detail)}</small></li>`).join(''),cl=r.claims.map(c=>`<article class="claim"><span class="pill ${c.status.toLowerCase()}">${esc(c.status.replaceAll('_',' '))}</span><h3>${esc(c.statement)}</h3><p>${esc(c.evidence)}</p></article>`).join(''),src=r.sources.map(s=>`<a class="source" href="${esc(s.url)}" target="_blank" rel="noreferrer"><span>${esc(s.source_type)}</span><b>${esc(s.title)}</b><small>${esc(s.publisher)} ↗</small></a>`).join('');result.innerHTML=`<div class="report-head"><p class="eyebrow">${esc(r.mode)} / ${r.iterations} iteration</p><h2>${esc(r.question)}</h2><p>${esc(r.executive_summary)}</p></div><div class="research-grid"><section><h4>Research trace</h4><ol class="trace">${ev}</ol></section><section><h4>Key claims</h4>${cl}</section></div><section class="facts"><h4>Established facts</h4><ul>${r.established_facts.map(x=>`<li>${esc(x)}</li>`).join('')}</ul><p><b>Important uncertainty:</b> ${esc(r.uncertainty)}</p></section><section class="sources"><h4>Source record</h4>${src}</section>`}
